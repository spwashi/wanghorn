import * as _ from './_';
import * as user from './user';
import * as file from './file';
import * as email from './email';

export const models = {
    _,
    file,
    user,
    email,
};

export default models;